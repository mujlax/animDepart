{
  "name": "my-electron-app",
  "version": "0.2.0",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package-intel": "electron-packager . AnimDepartScripts --platform=darwin --arch=x64 --out=dist_intel --overwrite",
    "package-arm": "electron-packager . AnimDepartScripts --platform=darwin --arch=x64 --out=dist_arm --overwrite"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^33.0.2",
    "electron-packager": "^17.1.2"
  },
  "dependencies": {
    "tinify": "^1.7.1"
  }
}
